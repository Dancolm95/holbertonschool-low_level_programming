#include "main.h"

/**
 * _abs - the function computes the absolute value of the integer argument
 * @n: The integer
 *
 * Return: value absolut of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
